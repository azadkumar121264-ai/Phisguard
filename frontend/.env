# Frontend environment variables (Vite)
# Only variables prefixed with VITE_ are exposed to the browser bundle.
#
# Local development: the Express backend in ../Backend listens on PORT || 5000
    VITE_API_URL=https://phishguard-etvk.vercel.app